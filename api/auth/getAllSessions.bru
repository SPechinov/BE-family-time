meta {
  name: getAllSessions
  type: http
  seq: 6
}

get {
  url: {{url}}/auth/get-all-sessions
  body: formUrlEncoded
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
