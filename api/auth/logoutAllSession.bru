meta {
  name: logoutAllSession
  type: http
  seq: 7
}

post {
  url: {{url}}/auth/logout-all-sessions
  body: formUrlEncoded
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

script:post-response {
  bru.setVar("authToken", undefined)
  
}

settings {
  encodeUrl: true
  timeout: 0
}
