meta {
  name: logoutSession
  type: http
  seq: 8
}

post {
  url: {{url}}/auth/logout-session
  body: formUrlEncoded
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

script:post-response {
  bru.setVar("authToken", undefined)
  
}

settings {
  encodeUrl: true
  timeout: 0
}
